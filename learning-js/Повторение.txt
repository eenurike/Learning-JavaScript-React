'\n' (перенос строки)

i % 2 === 0 (четные числа)

i % 2 === 1 (нечетные числа)

array[array.length - i] (массив будет ототбражаться задом наперед)

reverse() - массив будет ототбражаться задом наперед

if (a < 0) {
    a = a * -1;
}
Убираем минус у отрицательных чисел