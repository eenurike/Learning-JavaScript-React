'use strict';

function sayHello(name) {
  return `Привет, ${name}!`;
}

console.log(sayHello('Нурике'));

function returnNeighboringNumbers(num) {
  return [5, num, 7];
}

console.log(returnNeighboringNumbers(6));